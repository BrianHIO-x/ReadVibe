# 项目协作

ReadVibe 是 Flutter 编写的 Android 本地阅读器。本文件适用于整个仓库，具体任务以用户当前要求为准。

## 项目入口

- `lib/screens/`：书架、文字阅读和 PDF 阅读页面；`lib/screens/reader/`：文字阅读的排版、翻页和选区组件。
- `lib/controllers/`：阅读进度、搜索、编辑及选区等状态逻辑。
- `lib/repositories/`：持久化接口；`lib/services/`：本地存储、导入解析与平台服务。
- `lib/models/`、`lib/widgets/`、`lib/theme/`：数据模型、通用界面和主题。
- `android/`：Android 构建、签名和原生能力；PDF 引擎、通道适配与后台任务执行各自独立。

## 工作原则

- 先核对相关源码及调用关系，再修改。文档用于导航，功能与参数以当前实现为准。
- 使用当前会话的工作目录、命令环境和可用 SDK。项目根目录以 `pubspec.yaml` 和 `lib/` 的实际位置确认。
- 修改保持在任务范围内，保留用户的其他改动。文件编辑使用补丁。
- 页面通过已有接口访问持久化与原生能力，独立模块使用明确的参数和依赖。搜索展示使用通用结果契约，章节编解码与平台资源管理分别维护。
- 原生文档计算在工作线程完成，主线程仅回传结果。
- 阅读页改动需覆盖当前模式、阅读位置、选区手势和相关模式切换。
- 存储改动需保护原始文件、已保存数据与异步写入顺序。编辑和统计提交须匹配正文版本；阅读缓存按会话管理，旧章节载荷按清单引用和宽限时间回收。
- 正文处理保持本地执行；涉及对外传递内容的功能遵循用户的明确操作。
- 签名材料和密码属于本地私密配置。发布构建沿用项目配置的正式签名。

## 配置与验证

每次 Bug 修复或功能更新，公开版本按 `0.6.X` 递增，`pubspec.yaml` 中 `+` 之后的 Android 内部构建号同步递增；两者都只在该文件里维护，构建命令不再用参数覆盖。

发布说明中的 `SHA-256：` 行须取自实际上传的 APK。应用校验安装包时优先采用资产自带的摘要，该行是旧发布缺少摘要时的唯一依据。

正式 arm64 包通过 `tool/check_release_package.py` 检查体积预算、必需离线资源和内置完整字库。

校验通过的正式包一律从 `build/app/outputs/flutter-apk/` 复制进仓库根目录的 `dist/`，按 `ReadVibe-Android-v<公开版本>-arm64-v8a.apk` 命名，历史版本一并保留。该目录是本机的发布包存档，不纳入版本控制。

版本及 Dart 约束见 [pubspec.yaml](pubspec.yaml)，Android 构建及签名要求见 [应用构建配置](android/app/build.gradle.kts)，自动检查流程见 [工作流](.github/workflows/flutter.yml)。

验证与改动范围相匹配：文档核对事实、路径和链接；Dart/Flutter 改动做静态分析及相关行为检查；平台、依赖或发布改动执行相应构建。交付时说明实际完成的验证和仍待确认的部分。

文档简洁描述当前用途与操作，可变参数引用配置文件。功能变化时同步相关说明。

## 文档入口

- [项目介绍](README.md)
- [阅读与书架操作](docs/USAGE.md)
- [项目文件详解](docs/ARCHITECTURE.md)
