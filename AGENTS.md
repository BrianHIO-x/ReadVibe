# ReadVibe 仓库协作规则

本文件是 `1.ReadVibe_Project` 仓库的最高级项目规则，适用于根目录以及 `ReadVibe/` 内的应用代码、配置、文档和发布产物。`ReadVibe/AGENTS.md` 可以补充应用级细则，但不得与本文件冲突。

## 一、命令与文件修改

- 默认命令环境固定为 PowerShell 7（`pwsh.exe`）。
- 搜索文件和文本优先使用 `rg` 与 `rg --files`。
- 文本文件统一使用补丁方式修改。
- 保留用户已有且与当前任务无关的改动。
- 不使用会丢失用户工作的破坏性 Git 命令。

## 二、全部 Markdown 同步规则

- 任何代码、配置、资源、依赖、版本、构建方式、发布产物、用户交互或功能行为发生变化后，都必须逐一检查并同步修正仓库内全部 `.md` 文件。
- 全量检查必须覆盖仓库根目录、应用目录、`docs/`、平台占位目录以及以后新增目录中的 Markdown。
- 每个 Markdown 都要核对当前版本、功能描述、命令、路径、平台状态、验证结论、限制和交叉链接。
- 文档只描述当前实际存在的实现、规则、限制和验证状态，不保留已替换实现的故障过程、旧版本修复流水、无效功能说明或虚构结论。
- 新增、移动、重命名或删除 Markdown 时，必须同步更新本清单、根 README 的文档导航和所有相关链接。
- 交付前使用 `rg --files -g '*.md'` 重新盘点，不得依赖手写清单代替实际搜索。

当前 Markdown 清单：

- `AGENTS.md`
- `README.md`
- `ReadVibe/AGENTS.md`
- `ReadVibe/README.md`
- `ReadVibe/docs/UI_OPTIMIZATION.md`
- `ReadVibe/windows/README.md`

## 三、版本与产物

- 当前公开发布基线为 `v0.5.6`。
- 正式产物为 `ReadVibe/dist/ReadVibe-Android-v0.5.6-arm64-v8a.apk`。
- 用户可见版本号只能使用 `v主版本.次版本.修订版本` 三段式语义版本。
- `ReadVibe/pubspec.yaml` 的 `version` 只写三段式版本，不附加 Android 构建编号。
- Android `versionCode` 是独立递增的内部整数，不得出现在公开版本号、APK 文件名或发布标题中。
- APK 文件名固定为 `ReadVibe-Android-v主版本.次版本.修订版本-arm64-v8a.apk`。
- 发布时必须同步版本配置、全部 Markdown、构建命令、验证结论和 `ReadVibe/dist/` 中的最终 APK。

## 四、平台范围

- 当前唯一正式目标端是 Android `arm64-v8a`。
- 当前最低系统为 Android 8.0（API 26），用于支持本地 DOC 二进制文档解析。
- `ReadVibe/windows/` 仅用于说明平台状态，不是可构建工程。
- 未经用户明确要求，不扩大平台和 ABI 范围。

## 五、验证规则

- 当前项目不保留自动化测试目录、测试缓存和 `flutter_test` 依赖。
- 未经用户明确要求，不创建自动化测试，也不使用测试通过数量作为交付结论。
- Dart 或 Flutter 代码变更至少执行 `flutter analyze` 和 Android arm64 release 构建。
- 仅文档变更不强制重新构建 APK；如果文档改变版本、构建命令或产物结论，则必须重新核对相应配置和文件。

## 六、数据与交互约束

- ReadVibe 是本地离线阅读器，未经授权不得加入账号、云同步、远程上传或书籍正文网络传输。
- 阅读页必须保护当前章节、每章独立滚动位置、阅读比例和离章保存顺序。
- 菜单、主题、字体、排版和阅读模式变化不得让正文回到章节顶部。
- 阅读模式固定为“分章 / 仿真 / 滚动”三种，默认“分章”。
- 分章模式保持单章纵向滚动和左右平移切章，不得再混入仿真纸张渲染选项。
- 滚动模式必须让全文章节在同一纵向滚动流中连续衔接，并在跨章时更新当前章节和该章局部进度。
- 仿真模式按扣除纸页留白后的正文视口高度逐页推进，同章内翻页和跨章翻页共用同一分页、预览和进度模型。
- 选中仿真模式时必须展开“仿真翻页 / 平滑翻页”二级选项，默认“仿真翻页”，并持久化用户选择。
- 仿真模式每一页的正文视口顶部和底部通常分别预留约一行、最多接近一行半的当前排版高度；左上角使用小字显示章节序号和章节名。
- 仿真正文视口必须容纳整数行，章节标题、正文、段间距和章尾占位必须共用同一行高网格；恢复位置先吸附到完整行边界。
- 仿真分页按当前完整行起点增减一个真实正文视口，不得从一行文字中间裁切，也不得造成漏字、重复或页码漂移。
- 相邻章节预览必须先恢复自身保存位置再进入翻页画面；翻页完成后不得发生可见的二次跳转。
- 选择“仿真翻页”时，左滑前翻使用垂直直线折痕和直线纸边，当前页右侧沿直线折回；纸张不得出现弯曲、斜折或波浪边缘。
- 右滑后翻不得保留独立覆盖特效或旧翻页路径；必须让上一页作为运动纸页，以 `1 - progress` 严格反向复用左滑的同一套直线折页几何，从纸背逐步展开为完整正面。
- 双向运动纸页的背面都必须显示与该纸页一致的反向字迹：前翻对应当前页，后翻对应上一页。优先使用实际 `RepaintBoundary` 快照围绕垂直折痕做精确水平镜像；快照尚未完成时必须立即使用同章节、同滚动位置的镜像页面层兜底，不得出现纯色空白纸带。
- 仿真模式的横向翻页必须由原始指针位移跟踪驱动，超过横向阈值后立即进入翻页。进入翻页后必须阻止并清除正文 `SelectionArea` 的选区、工具栏和选择手柄；未发生横向翻页的静止长按仍交给系统文本选择。
- 仿真页面、同章预览页和相邻章节页必须使用与各自 `ScrollController` 绑定的唯一组件键，翻页提交不得错误复用其他页面元素。
- 分章或滚动模式与仿真模式互相切换时必须重新创建阅读控制器，在目标布局完成后按章节比例恢复并吸附到完整行；不得把另一种布局的像素位置无缝复用到仿真分页。
- 仿真翻页快照只服务于当前手势；滚动、菜单、设置、章节或手势状态变化后必须失效。
- 仿真翻页效果进入稳定页面后应主动异步预取当前屏快照；指针按下、拖动和回弹路径不得调用同步整页栅格化。后翻目标页进入预览树后异步捕获自身快照。快照未就绪时仍必须保持纸页裁切、折痕、阴影、透纸字迹、拖动、回弹和提交可操作，不能用捕获条件阻断翻页或切章。
- 选择“仿真翻页”时无论快照是否就绪都必须使用卷页裁切、折痕、阴影和纸背模型，不得回退成左右平移；“平滑翻页”只能由用户显式选择。
- 选择“平滑翻页”时不得捕获或预取纸背快照，但必须保留仿真模式的按屏分页、完整行边界、章节页眉和进度提交行为。
- 开书和关书共用主题安全的正反转场；深色阅读主题下不得出现浅色、白色或灰色错误中间帧。
- 转场资源必须保留到 `TransitionRoute.completed` 后再释放。
- 双击正文不得触发文本选中；长按菜单只显示复制、分享、全选、翻译和搜索。
- 翻译由 Android `PackageManager` 查询真实的 `ACTION_SEND / ACTION_PROCESS_TEXT / ACTION_TRANSLATE` 处理器，前端只保留已安装且能接收文字的 DeepSeek、ChatGPT、Gemini、Claude、Microsoft Copilot 和 Perplexity；原生启动前再校验白名单和系统返回的显式组件，发送内容固定包装为 `翻译如下内容：“所选文字”`。
- 搜索由 Android 查询真实的 `ACTION_VIEW` 网页处理器，前端固定只显示 `edge / chrome / 系统浏览器`；Edge 和 Chrome 使用系统返回的显式组件，系统浏览器使用未限定包名的原生 Intent。翻译与搜索都允许记住默认目标，下次直接打开，并在全局设置中提供重新选择入口。
- 翻译目标必须以可左右滑动的横向卡片显示，使用随 APK 内置的六个官方 AI 应用图标，不得使用通用机器人图标或单选圆点；搜索目标使用无图标的横向文字品牌卡，通过描边、字标和底部强调线表达选中状态。
- “系统字体”和“内置宋体”必须并列保留；优化字体包体时不得删减现有 Unicode cmap。
- 字重界面固定显示“细 / 中 / 粗”；内置宋体的“中”必须使用原“粗”的加粗效果，不得再与“细”显示为相同粗细。
- 阅读字号固定为 `16 / 18 / 20 / 22 / 24`，默认 `20`。
- 目录只在存在明确卷标题时显示卷与章节两级结构；卷外简介类内容作为顶部一级项；展开状态按书持久化。
- 目录每个可读章节的最右侧必须显示该章正文的非空白 Unicode 字数；统计在后台 isolate 中执行并写入轻量元数据，目录和开书不得等待结果。
- 阅读菜单顶部不得显示书名；必须显示当前章节序号和章节名，并随分章、滚动和仿真模式的活动章节同步更新。
- 书架手机布局每行固定显示三本书，书卡标题、格式标签和元信息必须在三列宽度内安全省略。
- 书架长按只打开“修改名称 / 移动 / 删除”三个选项；修改名称为主要操作，移动和删除使用紧凑的图标加小文字按钮。
- 选择“移动”后进入独立书架整理态；拖起书卡时保留原位占位，相邻书卡以弹性位移动画让位，松手后回弹并持久化顺序。普通书架长按不得直接启动拖动。
- 导入固定支持 TXT、EPUB、DOCX 和 DOC；DOCX 在 Dart isolate 内解析，DOC 在 Android 工作线程中本地提取，均不得上传正文。
- 全文字数在后台 isolate 中统计并持久化，不得阻塞书架、开书或目录操作。

## 七、交付检查

交付前至少确认：

1. 用户要求已经在实际实现中完成。
2. 适用的静态分析和 release 构建状态被准确记录。
3. 公开版本号、APK 文件名、ABI、大小和哈希来自实际产物。
4. `test/`、`.dart_tool/test/` 和 `flutter_test` 没有被擅自加入。
5. 全部 Markdown 已通过 `rg --files -g '*.md'` 逐一核对。
6. 文档没有旧版本流水、过期实现、错误平台状态、失效链接或互相矛盾的说明。
7. 如果调整内置字体，必须核对调整前后的 glyph 数、Unicode cmap 和实际 APK 体积。
