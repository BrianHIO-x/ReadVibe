# ReadVibe 仓库协作规则

本文件是整个 `1.ReadVibe_Project` 仓库的最高级项目规则，适用于仓库根目录以及 `ReadVibe/` 下的应用代码、配置、文档和发布产物。`ReadVibe/AGENTS.md` 可以补充应用级细则，但不得与本文件冲突。

## 一、命令环境

- 默认命令环境固定为 PowerShell 7（`pwsh.exe`）。
- 搜索文件和文本优先使用 `rg` 与 `rg --files`。
- 修改文本文件统一使用补丁方式，保留用户已有且与当前任务无关的改动。
- 不使用会丢失用户工作的破坏性 Git 命令。

## 二、全部 Markdown 同步硬性规则

- 任何代码、配置、资源、依赖、版本、构建方式、发布产物、用户交互或功能行为发生变化后，都必须逐一检查并同步修正仓库内全部 `.md` 文件，禁止只更新其中一部分。
- “全部 `.md`”包括仓库根目录、应用目录、`docs/`、平台占位目录以及以后新增目录中的所有 Markdown；不得因为文件不在当前工作目录而漏掉。
- 即使某个 Markdown 看似不受本次变更直接影响，也必须检查其中的版本号、功能描述、命令、路径、平台状态、验证结论、限制和文档索引。确认内容仍然正确后，才可视为完成核对。
- 新增、移动、重命名或删除 Markdown 时，必须同时更新本文档中的清单、根 README 的文档导航以及所有受影响的交叉链接。
- 交付前必须重新执行 Markdown 全量盘点，确认没有漏改、旧版本残留、失效路径、已删除功能、虚构验证结果或互相矛盾的说明。
- 当前需要逐一维护的 Markdown 清单：
  - `AGENTS.md`
  - `README.md`
  - `ReadVibe/AGENTS.md`
  - `ReadVibe/README.md`
  - `ReadVibe/docs/UI_OPTIMIZATION.md`
  - `ReadVibe/windows/README.md`

## 三、版本命名硬性规则

- 用户可见版本号只能使用三段式语义版本：`v主版本.次版本.修订版本`。
- 禁止在公开版本号后使用加号或其它后缀拼接 Android 内部构建编号。
- `ReadVibe/pubspec.yaml` 的 `version` 字段只写三段式版本，不附加构建编号。
- Android `versionCode` 是仅供系统判断升级顺序的内部整数，必须单独递增，不得出现在公开版本号、APK 文件名或发布标题中。
- APK 文件名固定为 `ReadVibe-Android-v主版本.次版本.修订版本-arm64-v8a.apk`。
- 发布时必须同步更新版本配置、全部 Markdown、构建命令、验证结论以及 `ReadVibe/dist/` 中的最终 APK。

## 四、平台与发布范围

- 当前唯一正式目标端是 Android `arm64-v8a`。
- 32 位 Android、Android x86/x86_64 模拟器、iOS、macOS、Linux 和 Web 当前不在维护范围。
- `ReadVibe/windows/` 只是占位说明，不是可构建的 Windows 工程。
- 未经用户明确要求，不扩大平台范围，不重新生成已删除的平台工程。

## 五、验证规则

- 当前项目不保留自动化测试目录、测试缓存和 `flutter_test` 依赖。
- 未经用户明确要求，不得重新创建自动化测试，也不得以自动化测试通过数量作为交付结论。
- Dart 或 Flutter 代码变更至少执行 `flutter analyze` 和 Android arm64 release 构建。
- 阅读手势、翻页、滚动位置、系统栏和动画问题应优先走 Android 真机操作路径；如果系统安全确认阻止自动安装或操作，必须如实说明，禁止把未完成的真机验证写成“已通过”。
- 仅文档变更不强制重复构建 APK，但必须完成 Markdown 全量一致性检查；如果文档修改了版本、构建命令或产物结论，则必须重新核对对应配置和产物。

## 六、数据与交互约束

- ReadVibe 是本地离线阅读器，不得在未授权情况下引入账号、云同步、远程上传或书籍正文网络传输。
- 修复阅读页时必须保护当前章节、每章独立滚动位置、阅读比例和离章保存顺序。
- 设置、菜单、主题、字体和翻页模式的重建不得让正文回到章节顶部。
- 相邻章节预览必须先恢复各自保存位置，再进入翻页画面；提交翻页不得出现可见的二次位置跳转。
- 双击正文不得触发文本选中；长按选择和复制仍需保留，除非用户另有要求。

## 七、交付检查

交付前至少确认：

1. 用户要求的行为已经实际实现，而不是只修改说明。
2. `flutter analyze` 和适用的 release 构建结果与文档描述一致。
3. 公开版本号、APK 文件名、ABI、大小和哈希来自实际产物。
4. `test/`、`.dart_tool/test/` 和 `flutter_test` 没有被擅自恢复。
5. 使用 `rg --files -g '*.md'` 重新取得完整 Markdown 清单，并逐一完成同步检查。
6. 所有 Markdown 之间不存在旧版本、旧测试数量、旧翻页实现、错误平台状态或失效链接。
